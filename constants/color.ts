// Legacy colors module kept for any template references.
// The active app uses ./theme.ts as the source of truth.
const tintColorLight = "#E85D3C";

export default {
    light: {
        text: "#1F1A17",
        background: "#F7F3EE",
        tint: tintColorLight,
        tabIconDefault: "#9A9088",
        tabIconSelected: tintColorLight,
    },
    dark: {
        text: "#F7F3EE",
        background: "#1F1A17",
        tint: tintColorLight,
        tabIconDefault: "#6B625A",
        tabIconSelected: tintColorLight,
    },
};
